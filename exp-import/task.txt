$problem = ea.p.totalOrder(
  qFunction = $qFun;
  example = ds.misc.simExample(
    simulation = ds.sat.fromEnvironment(
      environment = ds.e.navigation(arena = m.nth(n = -1; values = $validationArenas));
      dT = $dT;
      tRange = $tRange
    )
  )
)

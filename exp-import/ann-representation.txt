$mapper = ea.m.enhancedNds(
  of = ea.m.dsToNpnds(npnds = ds.num.mlp(innerLayers = [8]));
  windowT = 0.5
)

$initialRepresentation = ea.r.doubleString()

$seededRepresentation = ea.representation.seeded(
  of = $initialRepresentation;
  seeds = ea.misc.fromTextFile(
    path = "./results/final-best.base64";
    f = f.fromBase64()
  )
)

